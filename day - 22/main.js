// write a function which will take marks (number) as input and add 5 to that
// number and will return grace marks

function addGraceMarks(marks) {
  if (marks >= 35) {
    return marks;
  }

  const graceMarks = marks + 5;
  return graceMarks;
}

for (let i = 0; i <= 10; i++) {
  break;
  console.log("Hiii");
}
